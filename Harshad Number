#include <stdio.h>
int main() {
    int n, temp, sum=0;
    scanf("%d",&n);
    temp = n;

    while(n!=0) {
        sum += n%10;
        n/=10;
    }

    if(temp % sum == 0)
        printf("Harshad");
    else
        printf("Not Harshad");
}
